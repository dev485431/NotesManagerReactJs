import React from "react"

export default class Folders extends React.Component {
    constructor(props) {
        super(props)
    }

    render() {
        return (
            <div>folders</div>
        )
    }
}
